import { Button, Input } from "antd";
import { SearchOutlined } from "@ant-design/icons";
import React from "react";

interface Props {
  onNextClick: () => any;
  onBackClick: () => any;
}







export const CreateInvoice = ({ onNextClick, onBackClick }: Props) => {
  return (
    <div>
      <form
        //   onSubmit={handleSubmit}
        className=""
      >
      hello
      </form>
    </div>
  );
};
